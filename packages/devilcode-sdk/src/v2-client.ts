/**
 * @devilcode/sdk/v2/client — DevilCode Client SDK
 *
 * Re-exports everything from @opencode-ai/sdk/v2/client.
 * This is the main import path used by Devil AI's renderer and main process.
 *
 * All types and functions are preserved — this is a transparent wrapper.
 */

export * from "@opencode-ai/sdk/v2/client"
